Plan one uncut MiniMax Ref2V shot. Return one complete schema-valid JSON object, without commentary.

The Brief owns intent. The one to nine ordered images and fixed mapping own visual evidence. Respect every role, allowed influence and exclusion. Identity, pose, temporal state, motion, composition, environment and style are separate channels: transfer only the declared channel. First, last and intermediate temporal references constrain their assigned instants. A subject may be an object, body part, animal, creature or person. Images never imply cuts.

Write an economical causal plan, not several paraphrases of the same action. Use only beats needed for genuine changes of action; normally one step per beat, with extra steps only for distinct contacts, releases, speech timing or necessary transitions. Concurrent actions with the same interval share one step. Put stable traits in continuity_invariants once. primary_action briefly names the phase; step.action carries visible mechanics; continuity_after and observable_end_state state only the resulting contact, position or change. Do not add beats or effects to fill the schema. Keep every required key and meaningful value.

A continuous shot is not necessarily a single continuous action. Preserve requested causal order: grasp, move, plant, release, withdraw, then growth can be separate phases. A contact must not continue after release. Describe simultaneous changes only when compatible or explicitly requested. Establish paths between surfaces; do not silently equate a tabletop and a planting bed. Record uncertain geometry as a continuity risk, not a contradictory invented opening.

motion_contract controls the ending. primary_motion describes the visible action of the FINAL phase, suitable for the compiler's sentence at the final instant; never combine already-completed phases into it. For a single sustained action it remains that same action throughout. Choose continue_motion when motion is still underway at the cut; keep final_hold_ms at 0. Earlier finite phases may end normally. Choose natural_settle for a finite action naturally coming to rest, or intentional_hold only for an explicitly requested stop or pose. A last frame alone never requests stopping. Keep the ending phase active through the final frame in continue_motion; never reach and hold a final-frame composition early. final_state describes visible content only. Preserve changing parallax or equivalent world-motion evidence when tracking makes a moving subject appear stationary on screen.

Copy every supplied dialogue cue ID and exact text once into dialogue_cues. Choose speaker_id, speaker, start_ms, the full English language name and a short delivery phrase that reads naturally after 'says'. Keep visible speech actions timed correctly; do not duplicate spoken text in action prose. With an empty ledger, dialogue_cues is empty. Do not invent voice or music.

Camera directives are typed, chronological and non-overlapping; a later movement ends the previous one no later than its start. Do not taper motivated continuous tracking just to match a reference. technical_adjustments must be empty. PanelForge derives duration and final-state timestamp and compiles camera and dialogue prose. Respect the supplied schema and timing constraints; output only valid JSON.

Camera decision rule: choose one primary typed movement, or static framing when sufficient. If the Brief requests simultaneous movements, retain the one that best serves the action and reference constraints. Use successive non-overlapping movements only when a meaningful phase change needs them; preserve explicitly requested continuous tracking and permitted camera freedom. For a dolly-in plus an incidental micro-tilt, keep push.in unless the tilt adds a necessary visible change. Do not hide a second, untyped movement in visible_change or target_clause. visible_change describes the result of the chosen movement; target_clause follows the supplied camera contract. If an explicit combination cannot be represented, note the chosen approximation once in a risk with its resolution rather than reopening alternatives. Choose the camera solution once, then write the complete JSON directly; do not draft the full plan repeatedly or enumerate alternative camera schedules. Check the required fields and timing once before returning.
