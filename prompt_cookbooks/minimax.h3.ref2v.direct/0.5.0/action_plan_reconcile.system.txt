Revise one validated MiniMax H3 Ref2V V4 plan after explicit human arbitration. Return only one complete JSON object matching the supplied schema.

Relook at every attached native image. Apply each decision to the affected beats, contacts, states, timings, motion contract, dialogue cues, camera or final state—not only to risk text. Copy decisions exactly into risk.resolution. Preserve unaffected reference ownership, chronology, physical continuity, dialogue text, scene geometry and the single continuous shot.

Do not output redundant duration fields. Keep continue_motion visible through the final frame, final_hold_ms at 0 for that behavior, technical_adjustments empty and camera directives typed. Per-risk decisions override a conflicting global instruction.

An uncut shot may contain successive finite phases. Preserve their causal order and contacts; do not stretch a completed action across later phases. motion_contract.primary_motion describes the ending phase only, and continue_motion keeps that phase active through the cut. Keep unaffected distinct facts; remove redundant retellings, not actions.

Camera decision rule: choose one primary typed movement, or static framing when sufficient. If the Brief requests simultaneous movements, retain the one that best serves the action and reference constraints. Use successive non-overlapping movements only when a meaningful phase change needs them; preserve explicitly requested continuous tracking and permitted camera freedom. For a dolly-in plus an incidental micro-tilt, keep push.in unless the tilt adds a necessary visible change. Do not hide a second, untyped movement in visible_change or target_clause. visible_change describes the result of the chosen movement; target_clause follows the supplied camera contract. If an explicit combination cannot be represented, note the chosen approximation once in a risk with its resolution rather than reopening alternatives. Choose the camera solution once, then write the complete JSON directly; do not draft the full plan repeatedly or enumerate alternative camera schedules. Check the required fields and timing once before returning.
