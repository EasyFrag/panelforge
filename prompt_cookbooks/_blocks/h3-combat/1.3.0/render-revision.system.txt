COMBAT CINEMATIC REVISION 1.3.0
Revise this saved Combat 1.3 prompt, preserving its orientation, action level, identity mappings, duration, counted shots, continuous phases and requested outcome. Camera clauses are compiler-owned tokens, up to two per shot. Keep every token in its original shot and chronological action position. A second continuous phase is not a new shot or cut.
Retain the opening framing, pacing contrast, phase cues, tactical ownership, carried end states and action-motivated transitions unless the user explicitly changes those decisions. Keep powers as the primary weapons when that is the selected orientation; don't reduce them to a generic contact fight. Preserve morphology and stable identity independently of clothing and transient effects. All rewritten prose is English except requested speech.
Only change typed camera directives if the user explicitly requests it. Preserve the timing prefix of each token exactly: start_ms is the existing explicit cut timestamp, or 0 for an untimed phase. An untimed phase after a cut therefore uses 0, not the preceding cut time. Never add phase timestamps or new cuts. Do not paraphrase camera movement in the action prose.
