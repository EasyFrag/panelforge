CALL 1 OF 2: DIRECTION PLAN
Return only the JSON matching the supplied schema. There is no preceding Brief and no hidden planning call. Use the real intention, images, roles and saved controls to decide the encounter. The worked example teaches the contract and connected mechanics; it does not override this encounter's duration, shot count, identity, genre, camera or outcome.
Create 1-6 shots according to the requested count (Auto may choose). Durations sum to REQUESTED DURATION MS. Each shot develops combinations, not a single gesture. Plan no more than two continuous phases per shot and at least 500 ms per phase. Prefer one phase when the shot is short or one trajectory already serves the action. Do not force every shot into the same duration or camera pattern.
Each phase contains a visible action cue, a typed camera directive, and linked exchanges. A second phase inherits the action and position of the first, without a cut or teleportation. A cue is a complete English sentence, not a timestamp. Track the tactical ownership of every attack. Give powers their origin, path, target, consequence and adversary response; distance and cover matter without requiring a punch.
opening_composition describes only static opening framing and positions. camera alone describes movement; use motion, optional amplitude/speed, and a short English target_clause. Do not paraphrase a camera movement in exchanges, cue, pacing, end_state or transition. For static_shot set amplitude and speed to null. For shake.slightly, shake.strongly and pov set amplitude/speed to null and target_clause to "". Other motions allow speed slow/fast or null, amplitude small/large or null. A target begins with a spatial continuation such as beside, above, below, toward, around, along, across, following, keeping, as or while. It contains no camera-control words (including arc, track, roll, orbit, crane). For an electrical attack, write 'beside the lightning creature', not 'following the electric arc'.
Use a tracking phase beside an advancing weapon, an arc around crossing fighters, or a vertical phase following a leap when the action justifies it and camera freedom permits. Do not invent unsupported compound enums. A second typed phase can change height or direction continuously; an internal snap-cut or insert requires its own counted shot instead.
pacing describes the action's rhythm in a complete English sentence, including any brief resistance, acceleration or contrast. end_state carries positions, momentum, weapon ownership and consequences into the next opening; it is not a freeze instruction. transition names the visible event bridging to the next counted shot, or the intended final action in the last shot. The application inserts these fields verbatim into the final prompt. Do not use placeholders such as 'same', 'as above', 'continue' or French shorthand.
Keep spoken_lines as the exact approved speech ledger. Any speech in exchanges uses canonical <d>[English] ...</d> (or the requested language); do not invent more speech than allowed. Audio follows the visible action. Music stays N/A when disabled.
