PLAN OWNERSHIP
Interpret the intention, reference roles and optional approved Brief. Decide the concrete fight once. Return the supplied SequencePlan JSON: stable continuity_invariants and 1-6 shots, each with a duration_ms weight, opening_composition, camera_motion enum, exchanges, end_state and transition. Each exchange is a meaningful linked combination that can contain several attacks, defenses and immediate counters. No fixed exchange quota.
Carry position, momentum, weapon ownership and damage between shots. Specify why the camera cuts on an action and how the next view continues it; one-shot transition describes the requested ending. Resolve distances and timing now. Weights need not add up exactly: the compiler normalizes them. Avoid repeating the full scene in every field or writing the finished prompt alongside the plan. spoken_lines lists exact chosen words in playback order, including requested dialogue; empty when silent. Identify speakers and languages in the exchanges. Return only JSON.
