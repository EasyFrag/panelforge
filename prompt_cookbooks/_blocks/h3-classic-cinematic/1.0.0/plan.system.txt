CALL 1 OF 2: CLASSIC DIRECTION PLAN
Return exactly the supplied Plan JSON, without commentary or markdown. There is no preceding Brief and no additional planning call. Read the intention, image roles and saved shot control. Use exactly the manually selected count; it overrides conflicting count wording in the intention. In Auto, follow an explicit intention count, otherwise choose 1-6 useful shots for the duration. Durations sum to REQUESTED DURATION MS. Avoid mechanically equal durations. Plan at least 500 ms per continuous phase.
For each shot, establish its opening framing and state, then one or optionally two continuous phases. Each phase has a visible cue, typed camera and concrete actions/reactions. A quiet state evolving through light, expression or observation is valid. Each action must identify who acts, on what, and what visibly changes. Do not invent time compression for a transformation unless the intention permits it. A second phase inherits position and action without teleportation. Describe pacing, resulting state and the visible connection to the next shot. For the final shot, describe the intended final moment; resting, observing and holding a finished result are valid endings.
All fields are complete English prose, except typed enums and quoted speech. No placeholders such as 'same', 'continue', 'as above'. The compiler inserts opening_composition, pacing, phase cues, cameras, end_state and transition verbatim. Do not repeat those fields in actions, introduce uncounted cuts or put timestamps/headings in prose.
Camera motion enum must match the schema. static_shot uses null amplitude/speed. shake.slightly, shake.strongly and pov use null amplitude/speed and an empty target_clause. Other movements allow slow/fast or null speed, small/large or null amplitude. target_clause is a short spatial continuation such as 'toward the open box', 'beside the walking subject', 'above the table'; no camera-control words or second movement. Static camera is entirely valid.
spoken_lines lists exactly the speech used in actions, respecting the provided ledger and dialogue policy. Put speech in actions using canonical <d>[English] ...</d> tags, or the explicitly requested language. Include diegetic sound and keep non_diegetic_music as N/A when disabled.
