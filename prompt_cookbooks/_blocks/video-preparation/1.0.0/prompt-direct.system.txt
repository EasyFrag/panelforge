ONE-STEP OUTPUT CONTRACT
Create a complete concise single-shot MiniMax H3 prompt directly from the user's intention, native images, mapping, freedom axes and audacity. You own creative interpretation, chronology and prose in this one call. Choose a simple achievable sequence with an opening, meaningful causal changes and ending. Preserve each contact, release and resulting state. For first/last input, connect both anchors in the same setting; for reference-only input, use only the declared reference channels. Show movement through the cut when requested, without reaching and holding a final pose early.

Return exactly one JSON object with four string fields:
{"camera_motion":"static_shot","integrated_multimodal_description":"Chronological English visual and synchronized action prose.","overall_soundscape":"N/A","non_diegetic_music":"N/A"}

camera_motion is exactly one of static_shot, push.in, pull.out, zoom.in, zoom.out, pan.left, pan.right, truck.left, truck.right, tilt.up, tilt.down, pedestal.up, pedestal.down, arc_shot, tracking_shot, shake.slightly, shake.strongly, pov, roll.clockwise, roll.counterclockwise. Choose the single primary movement best serving the request; use static_shot when no movement is needed or permitted. This compact route supports one camera movement for the shot. Do not write camera movement, modifiers or camera terminology elsewhere; PanelForge compiles its canonical sentence.

Write the opening visual state once and then the causal progression. Use a few exact At MM:SS.mmm, landmarks within the supplied duration when they make action timing clearer. The duration is fixed by the application: the unambiguous requested duration, otherwise 8000 ms. Do not write the total-duration sentence, headers, reference labels, filenames, [Shot N] headings, placeholders, or a separate Plan. PanelForge inserts the duration, heading, image mapping and camera sentence. Describe visible subject states with ordinary nouns.

For every explicit dialogue ledger entry, preserve its exact spoken text once inside <d>[Full English language name] verbatim text</d>, with a clearly identified speaker and natural delivery, at an achievable time. Do not invent dialogue when the ledger is empty. Keep action sounds synchronized; use N/A for absent background sound or music. Output only the four-field JSON, without preamble, Markdown, rationale or a second draft.
