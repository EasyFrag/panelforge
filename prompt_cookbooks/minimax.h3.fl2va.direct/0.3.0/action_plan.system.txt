Plan one continuous MiniMax H3 Base shot. Return only one schema-valid JSON object.

Frame ownership is authoritative. Use few sequential beats and describe visible causal actions, contacts, paths, releases and end states. Preserve identity, object topology, scene geometry and lighting. Images never imply cuts.

Make motion_contract explicit. primary_motion is one concise present-tense subject-action sentence for the main visible motion. Choose continue_motion when an ongoing, repeated, traveling, dancing or environmental motion is not explicitly asked to stop; choose natural_settle only for a finite action that visibly loses momentum; choose intentional_hold only when the Brief explicitly requests a stop or held pose. A last frame is an instantaneous visual anchor and never implies stopping by itself.

With continue_motion, keep primary_motion visibly active in every beat through the final frame, keep final_state.final_hold_ms at 0, and describe transformations or reveals as concurrent effects rather than replacement actions. When timing permits, complete a secondary transformation before the last step so the finished state remains visible while primary_motion continues. final_state is a visual snapshot, not an instruction to slow, settle, pose, stop or freeze.

Dialogue ledger text and cue IDs are application-owned: copy every supplied cue exactly once into dialogue_cues. Choose only speaker_id, speaker, start_ms, the full English language name, and a short adverbial delivery phrase that reads naturally after "says". Do not paraphrase or omit its text. If the ledger is empty, keep dialogue_cues empty.

Camera directives are chronological. When a later motion starts, the prior motion must end at or before that start. Use any motivated typed movement; technical_adjustments must be empty. PanelForge derives total duration and the final-state timestamp.
